# CHANGE NOTE: Default backend port is now 3002 (configured via PORT env).
# Procfile delegates to start.sh which binds 0.0.0.0:${PORT:-3002}
# IMPORTANT: Many platforms (Heroku/preview systems) inject PORT. If they set PORT=3001,
# the service will correctly bind to 3001. Update your platform config/health checks to match.
web: bash start.sh

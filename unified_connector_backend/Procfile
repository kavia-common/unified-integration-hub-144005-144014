# CHANGE NOTE: Default backend port is now 3002 (configured via PORT env).
# Procfile delegates to start.sh which binds 0.0.0.0:${PORT:-3002}
# IMPORTANT: Many platforms (Heroku/preview systems) inject PORT (often 3001).
# The service will bind to the injected PORT. Ensure your platform config/health checks use the same port.
web: bash start.sh

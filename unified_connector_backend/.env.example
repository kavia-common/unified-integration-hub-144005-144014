# Server
HOST=0.0.0.0
PORT=3001
LOG_LEVEL=info
RELOAD=false
ALLOWED_ORIGINS=*

# Optional: AES-GCM encryption key (base64 or hex or raw). If unset, credentials stored plaintext in memory.
# ENCRYPTION_KEY=base64:AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA=

# Optional OAuth demo variables (not required for mocked flow)
# JIRA_CLIENT_ID=
# JIRA_CLIENT_SECRET=
# JIRA_REDIRECTION_URI=http://localhost:3001/api/connectors/jira/oauth/callback
# CONFLUENCE_CLIENT_ID=
# CONFLUENCE_CLIENT_SECRET=
# CONFLUENCE_REDIRECTION_URI=http://localhost:3001/api/connectors/confluence/oauth/callback

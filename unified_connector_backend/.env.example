# Example environment configuration for Unified Connector Backend
# Copy this to .env and adjust as needed (do NOT commit .env with secrets).

# Host to bind; use 0.0.0.0 inside containers
HOST=0.0.0.0

# Port to bind; the app defaults to 3002 if not set.
# IMPORTANT: Many preview/deploy platforms inject PORT (often 3001). The server will bind to the injected value.
# Ensure your health checks and port mappings use the same value.
PORT=3002

# Log level for uvicorn: critical, error, warning, info, debug, trace
LOG_LEVEL=info

# Enable auto-reload (development only). Set to "true" for local dev.
RELOAD=false

# CORS allowed origins (comma-separated). Leave empty for "*"(all) in development.
# Example: ALLOWED_ORIGINS=http://localhost:3000,https://your-preview-url.example
ALLOWED_ORIGINS=
